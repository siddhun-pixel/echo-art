# Echo Art 🎨

Echo Art is a Flask-based web application that lets users:
- Convert **voice recordings to text** using SpeechRecognition.
- Generate **art/image outputs** (currently placeholder text, can be extended later).
- Interact through a simple frontend with text input, audio upload, and output display.

---

## 🚀 Project Structure
echo-art/ │ ├── app.py                  # Flask backend ├── requirements.txt        # Python dependencies ├── templates/ │   └── index.html          # Frontend HTML ├── static/ │   ├── style.css           # CSS styling │   └── script.js           # JS frontend logic └── README.md               # Project documentation





